<form method="post" action="{{ path('app_puesto_delete', {'id': puesto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ puesto.id) }}">
    <button class="btn">Delete</button>
</form>
