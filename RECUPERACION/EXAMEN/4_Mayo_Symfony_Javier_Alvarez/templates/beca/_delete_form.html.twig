<form method="post" action="{{ path('app_beca_delete', {'id': beca.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ beca.id) }}">
    <button class="btn">Delete</button>
</form>
